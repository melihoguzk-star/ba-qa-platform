# BA-QA Platform Environment Variables

# =============================================================================
# API Keys
# =============================================================================

# Anthropic API Key (for Claude)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google Gemini API Key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# n8n Webhook URLs - Google Drive Integration
# =============================================================================

# n8n webhook for Google Docs OAuth token
# Returns OAuth token for accessing Google Docs
N8N_GOOGLE_DOCS_WEBHOOK=https://sh0tdie.app.n8n.cloud/webhook/google-docs-proxy

# n8n webhook for Google Sheets OAuth token
# Returns OAuth token for accessing Google Sheets
N8N_GOOGLE_SHEETS_WEBHOOK=https://sh0tdie.app.n8n.cloud/webhook/google-sheets-proxy

# Optional: n8n webhook API key for security
# N8N_WEBHOOK_API_KEY=your_secret_key_here

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite database path (default: data/ba_qa_platform.db)
# DATABASE_PATH=data/ba_qa_platform.db

# =============================================================================
# Application Settings
# =============================================================================

# Debug mode (set to true for development)
# DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# =============================================================================
# Phase 2A: Semantic Search & Vector Database Configuration
# =============================================================================

# Embedding model for semantic search
# Default: intfloat/multilingual-e5-base (Turkish + English support)
EMBEDDING_MODEL=intfloat/multilingual-e5-base

# ChromaDB vector store persistence path
CHROMA_DB_PATH=data/chroma_db

# Maximum batch size for embeddings
CHROMA_MAX_BATCH_SIZE=32

# Feature flags
# Enable semantic search functionality (true/false)
ENABLE_SEMANTIC_SEARCH=true

# Enable automatic indexing of new documents in ChromaDB (true/false)
ENABLE_AUTO_INDEXING=true

# Similarity threshold for semantic search results (0.0-1.0)
# Higher values = more strict matching
SIMILARITY_THRESHOLD=0.7

# =============================================================================
# Phase 2B: Hybrid Search Configuration
# =============================================================================

# Enable hybrid search (TF-IDF + Semantic fusion)
ENABLE_HYBRID_SEARCH=true

# Hybrid search alpha parameter (0.0-1.0)
# - 0.5 = balanced (50% keyword + 50% semantic)
# - 0.7 = prefer keyword matches (70% keyword + 30% semantic)
# - 0.3 = prefer semantic matches (30% keyword + 70% semantic)
HYBRID_SEARCH_ALPHA=0.6

# Fusion method: 'weighted' or 'rrf' (Reciprocal Rank Fusion)
# - weighted: Simple weighted combination (faster)
# - rrf: Advanced ranking fusion (better quality)
HYBRID_FUSION_METHOD=weighted

# Maximum number of search results to return
SEARCH_RESULT_LIMIT=20

# Show chunk preview in search results (true/false)
SHOW_CHUNK_PREVIEW=true

# =============================================================================
# Instructions
# =============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your actual values (remove the placeholders)
#
# 3. Never commit .env to version control (it's in .gitignore)
#
# 4. For production deployment, set these as environment variables
#    instead of using a .env file
#
